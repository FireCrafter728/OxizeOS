// SPDX-License-Identifier: GPL-3.0-or-later

#pragma once

template <typename T>
T min(T val1, T val2)
{
	return val1 < val2 ? val1 : val2;
}

template <typename T>
T max(T val1, T val2)
{
	return val1 > val2 ? val1 : val2;
}